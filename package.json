{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "deploy": "vite build && mkdir -p dist/build dist/fonts && cp -R public/build/ dist/build/ && cp -R public/images/ dist/images/ && cp -R public/fonts/ dist/fonts/ && cp public/favicon.png dist/favicon.png",
        "dev": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "@types/node": "^24.10.2",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "gsap": "^3.14.1",
        "laravel-vite-plugin": "^2.0.0",
        "swiper": "^12.0.3",
        "tailwindcss": "^4.0.0",
        "typescript": "^5.9.3",
        "vite": "^7.0.7"
    },
    "dependencies": {
        "lucide": "^0.575.0"
    }
}